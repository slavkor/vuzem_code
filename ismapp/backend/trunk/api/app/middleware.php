<?php
use League\OAuth2\Server\ResourceServer;

use \App\Middleware\StatisticsMiddleware;
use \App\Middleware\AuditMiddleware;


//$app->add(new StatisticsMiddleware($app->getContainer()->get('settings')));
//$app->add($app->getContainer()->get(AuditMiddleware::class));

